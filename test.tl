global function add(x: integer, y: integer): integer
    local result: integer = x + y

    return result
    -- return x + y
end

global function my_func(x: integer): integer
    local var = x * 12
    return var + x
end
